// Single source of truth for brand contact details. Replace these with the
// real numbers before launch — they are referenced by the bespoke pages and
// the checkout flow.
export const WHATSAPP_NUMBER = "919800000000"; // digits only, country code first
export const WHATSAPP_DISPLAY = "+91 98000 00000";
export const WHATSAPP_LINK = `https://wa.me/${WHATSAPP_NUMBER}`;
